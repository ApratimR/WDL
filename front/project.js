function send()
{
	//gets the text from the text area into client side
	var input_string = document.getElementById("strinput").value;
	var length_in_str = input_string.length;
	console.log(length_in_str);
}

